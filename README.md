### Ideas around creating a component library with react and tailwind

Getting started:

- `yarn`
- `yarn storybook`

### To update the tailwind css, open a second terminal and run:

- `npx tailwindcss -i ./src/index.css -o ./dist/output.css --watch`
